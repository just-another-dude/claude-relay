# Optional — group ID to restrict to (e.g., 123456789012345678@g.us)
# Leave empty to use "Message yourself" chat instead
# To find group ID: send /groupid in the group after starting the relay
ALLOWED_GROUP_ID=

# Required when using group mode — your phone number (no + or spaces)
# Only messages from this number will be processed in the group
ALLOWED_NUMBER=

# Optional — for /ask command (direct API queries)
ANTHROPIC_API_KEY=

# Optional — defaults shown
CLAUDE_MODEL=claude-sonnet-4-20250514
TMUX_SESSION=claude-relay
CLAUDE_WORKSPACE=~/claude-workspace
READ_TIMEOUT=30
MAX_OUTPUT=3000

# Voice transcription settings
TRANSCRIBER_PATH=~/git/audio-transcriber
TRANSCRIBER_ENGINE=google
